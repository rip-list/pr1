# gh-pages.yaml
flutter:
  # путь к папке с вашим проектом Flutter Web
  # находится внутри папки `web` в корне проекта
  target: web

# Опционально: вы можете настроить другие параметры, такие как имя ветки
# или путь к папке с собранными файлами, если он отличается от значения по умолчанию
gh_pages:
   branch: webadaptive
#   build_dir: build/web