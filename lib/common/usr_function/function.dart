// ignore: unused_import
import 'package:pr1/common/widgets/float_bar.dart';

Future<void> onPresedSwitchButton() async {
  const bool kDebugMode = true;
  if (kDebugMode) {
    // ignore: avoid_print
    print("button dell presed");
  }
}
